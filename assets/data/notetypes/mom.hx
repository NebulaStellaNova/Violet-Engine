function noteHit(event) {
    if (event.noteType == "mom") {
        event.animationSuffix = "alt";
    }
}

function sustainHit(event) {
    if (event.noteType == "mom") {
        event.animationSuffix = "alt";
    }
}