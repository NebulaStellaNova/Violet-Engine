# How To Compile The Engine

If you're on windows:<br>
Run `setup.bat`.

Otherwise:<br>
Run `setup.sh`.

After that open a terminal/cmd in the engine's folder, then type `lime test windows` and press enter.
